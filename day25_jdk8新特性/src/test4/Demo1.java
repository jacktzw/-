package test4;

import java.util.ArrayList;
import java.util.stream.Stream;

public class Demo1 {
    public static void main(String[] args) {
        /**
         * jdk8的新特性：Stream流
         * 用于解决集合类库既有的弊端
         */
  /*      ArrayList<String> list=new ArrayList<>();
        list.add("张三");
        list.add("李四");
        list.add("张三三");
        list.add("赵六");
        list.add("张一一");
        System.out.println(list);
        list.stream().filter(t->t.contains("张")).filter(b->b.length()==3).forEach(s-> System.out.println(s));
        Stream<String> stream = list.stream().filter(t -> t.contains("张"));
        System.out.println(stream);*/
    }
}
